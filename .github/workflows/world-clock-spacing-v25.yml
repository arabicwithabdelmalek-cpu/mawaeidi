name: World clock spacing v25

on:
  push:
    paths:
      - '.github/workflows/world-clock-spacing-v25.yml'

permissions:
  contents: write

jobs:
  patch:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Apply world clock spacing update
        run: python scripts/world_clock_spacing_v25.py
      - name: Validate inline JavaScript
        run: |
          python - <<'PY'
          import re
          from pathlib import Path
          text = Path('index.html').read_text(encoding='utf-8')
          blocks = re.findall(r'<script(?:\s[^>]*)?>(.*?)</script>', text, flags=re.S|re.I)
          Path('/tmp/mawaeidi-inline.js').write_text('\n'.join(blocks), encoding='utf-8')
          PY
          node --check /tmp/mawaeidi-inline.js
      - name: Commit spacing polish
        run: |
          rm -f .github/workflows/world-clock-spacing-v25.yml scripts/world_clock_spacing_v25.py
          git add -A
          git config user.name 'github-actions[bot]'
          git config user.email '41898282+github-actions[bot]@users.noreply.github.com'
          git commit -m 'Tighten world clock spacing and action contrast' || exit 0
          git push
